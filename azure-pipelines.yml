trigger:
  branches:
    include:
      - main
      - updates

pr:
  branches:
    include:
      - main

pool:
  vmImage: ubuntu-latest

variables:
  - group: kzn-skills-audit-portal

stages:

# =========================
# DEPLOY FRONTEND (NO BUILD ✅)
# =========================
  - stage: DeployFrontend
    displayName: Deploy frontend
    jobs:
      - job: DeployStaticWebApp
        displayName: Deploy to Azure Static Web Apps
        steps:
          - checkout: self

          # ✅ flatten dist contents (fix assets loading)
          - script: |
              mkdir app
              cp -r dist/* app/
            displayName: Prepare deployment files

          - task: AzureStaticWebApp@0
            inputs:
              app_location: 'app'
              output_location: ''
              skip_app_build: true   # ✅ critical
              azure_static_web_apps_api_token: '$(AZURE_STATIC_WEB_APPS_API_TOKEN)'


# =========================
# DEPLOY POWER PLATFORM ✅
# =========================
  - stage: DeployPowerPlatform
    displayName: Deploy Power Platform assets
    jobs:
      - job: DeployDataverse
        displayName: Provision Dataverse schema and relationships
        steps:
          - checkout: self

          - task: PowerShell@2
            inputs:
              targetType: inline
              script: |
                pwsh ./scripts/deploy-dataverse-schema.ps1 `
                  -TenantId '$(TenantId)' `
                  -ClientId '$(ClientId)' `
                  -ClientSecret '$(ClientSecret)' `
                  -DataverseUrl '$(DataverseUrl)'

                pwsh ./scripts/deploy-dataverse-relationships.ps1 `
                  -TenantId '$(TenantId)' `
                  -ClientId '$(ClientId)' `
                  -ClientSecret '$(ClientSecret)' `
                  -DataverseUrl '$(DataverseUrl)'

                pwsh ./scripts/deploy-dataverse-keys-and-choices.ps1 `
                  -TenantId '$(TenantId)' `
                  -ClientId '$(ClientId)' `
                  -ClientSecret '$(ClientSecret)' `
                  -DataverseUrl '$(DataverseUrl)'
              pwsh: true